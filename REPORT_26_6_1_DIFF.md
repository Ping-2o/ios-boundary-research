> **NOTICE — AI-GENERATED SECURITY RESEARCH.** This document was written 100% by
> autonomous AI agents (no human authoring or line-by-line review pass — expect slop).
> It exists solely for authorized security research and coordinated disclosure to the
> affected vendor. All claims cite checkable artifacts (offsets, receipts, commands);
> re-verify before trusting any of them. PoC files are minimal triage reproducers,
> not weapons. Do not use against systems you do not own or may not test.

# REPORT_26_6_1_DIFF — iOS 26.6 (23G71) → 26.6.1 (23G83) full security diff audit
**(09-04, Ghidra-agent pipeline: 449/449 blacktop diff MDs triaged + binary-level disasm diff of every real change. Data: `.ds_2661_diffs/` (all per-binary MDs + `_triage.json`), binaries `.ds_2661_work/` (kext71/83, iio/ne/eap/ccd/wc/fnd/cf/atb/ct/fp/aa/lm ×71/×83), kernelcaches kc71/kc83.)**

**TL;DR: 26.6.1 is a remote-campaign-response patch. 8 real security fixes, 0 in the user's "sandbox/IPC mitigation" row (launchd/dyld/xpcproxy/libsandbox/AMFI are build-stamp-only rebuilds). THE DEVICE IS ON 23G71 = ALL FIXES BELOW ARE LIVE 0-DAYS ON IT.**

| # | Component | OLD bug (disasm-proven) | NEW guard | Reach | Value |
|---|---|---|---|---|---|
| 1 | **ImageIO `_CGImageCreateByScaling`** (+`IIOPixelProvider::iterateOverImage`) = **CVE-2026-65346** | `mul w22, w27, w8` — dstWidth*bpp **32-bit wrap** → undersized `malloc` → scaler fills full rows = **controlled heap overflow, attacker-controlled content** | 64-bit `mul x9` + `tst #-0x100000000` + round-up carry check → NULL | any scaled decode (thumbnails, WebKit `<img>`) — fully remote 0-click-adjacent | **ACE-class** |
| 2 | **NetworkExtension IKEv2/EAP** = **CVE-2026-65329** | `createInitiatorEAPAuthenticationDataUsingPrimeKey:` + `validateAuthPayloadAsInitiator:beforeEAP:` substitute an **all-zero 232/240B key** when `-[NEIKEv2EAP sessionKey]` is nil → AUTH "verifies" against a public constant, BOTH directions | new `-[NEIKEv2EAP shouldHaveSessionKey]` (state==5 gate); "eapClient.sessionKey failed" → nil AUTH / SA→AUTHENTICATION_FAILED | network-adjacent, pre-auth (rogue VPN concentrator / Wi-Fi-calling MITM) | **pre-auth auth bypass → tunnel MITM** |
| 3 | **EAP8021X** `eapsim_notification`/`eapaka_notification` (same CVE family) | `EAPSIMAKAKeyInfoVerifyMAC` called with **no key-availability check** → zero-key HMAC = any AT_MAC "verifies" → unauthenticated server-directed Notification drives the SIM/AKA auth state machine | new `keyInfo->has_keys` (+0xf0) flag check | rogue 802.1X/WPA-Enterprise AP | auth-state confusion/DoS |
| 4 | **libCommCenterKCommandDrivers** `IBI_STK_CommandDriver::envelopeIMSRegistration` | **unbounded join** of carrier-controlled IMS-registration strings into the fixed ~0xf8B STK envelope (memcpy cursor `1+Σ(len+2)`, u8 len byte) → **baseband→AP heap overflow** | projected-cursor `cmp x9, 0xf2` + "envelopeIMSRegistration truncated: %s" + new `ctu::join` | hostile carrier / SIM content | heap overflow, carrier-adjacent |
| 5 | **libAppleArchive** extraction | hardlink **donor type unchecked** (`_link` no stat → donor can be a symlink planted by an earlier entry = **tar-slip arbitrary-file-write**); "Invalid segment size in cluster header" (OOB read); "blob field size too large" / "header payload too large" / "cluster_id out of range" (realloc-drive) | both endpoints stat'ed S_IFMT-checked, `_link`→**`_linkat`**, 1<<48 blob ceiling, cluster-table state checks | any .aea/.aar consumer (Files, Safari, restore, MDM) | **persistence/delivery primitive** |
| 6 | **AppleLockdownMode** ACM deserializers (`_LibSer_SEPControl_Deserialize` 352→492, `_LibSer_SEPControlResponse` 208→288, `_DeserializeCredential` +1 insn) | (1) cmdSize length-prefix read at `buf+consumed` **unchecked** + unvalidated (ptr,len) output window; (2) **respSize trusted raw from header** → 4GB over-read window; (3) SEP-credential object returned with **stale heap dataLength** (missing zero-init → type-confused payload) | `remaining >= sizeof(u32)/cmdSize/respSize` assert lattice → new err -0x10; `str wzr,[x8,8]` zero-init | SEP↔XNU ACM trust boundary (entitled daemon / chained) | boundary hardening |
| 7 | **AppleSEPCredentialManager** `TRMMultiState_ReplaceInBuffer` (dispatcher opcode 0x1a, payload ≤4096) | record/tag walker with **no per-item bounds** → OOB read of input + replace/shift **memmove OOB write** into cached multi-state buffer | full containment lattice (`readPos+payloadSize<=inBufferSize`, `newTotalSize<=bufCapacity`, `pos==curSize`, `numRecords<=CacheSize`, `copied==srcLen`), +1 fn | same boundary | same |
| 8 | **hfs** `HeadTruncateFile` (hfs_relocate path) | loop-2 tail-extent staging `w27++` **unbounded** into 8-entry **stack buffer** → **kernel stack OOB write** on fragmented-file truncate | `cmp w23,#7; b.gt` → "too many tail extents" + **mark volume inconsistent** | local user | kernel stack smash → fail-safe |
| 9 | **lifs** `_lifs_request_done` | outBuf==NULL with copyin flag set → **kernel NULL-deref via copyin** (or silent false-success) | "we got no buffer to copyin…returning EIO" | FSKit provider (entitlement-gated) | provider-boundary |
| 10 | **AppleAVD** `resetFrameQslot` (avdOutbox0ISR→HardReset→dropDecodeCommand) | **no bufIdx range check** (OOB/negative index into 8-slot arrays) + **unconditional in-flight `--`** on an already-AVAILABLE slot = counter underflow + **premature AVAILABLE marking of a frame the HW still DMAs** | `cmp w1,#8` reject; skip-reset on AVAILABLE ("avoid in-flight underflow"); counter-- only on real transition | decode sessions via videocodecd (race/error paths — **row-K adjacent**) | frame-buffer UAF class |
| 11 | **AudioToolbox** `AudioQueueXPC_Server::SetProperty` 652→1136 + `_AUIOServer_SetProperty` 1464→1656 | **no caller authorization** on queue/RemoteIO properties — any local process can spoof `kAudioQueueProperty_ClientAuditToken` / flip `SetCanRecord` (mic) | 32-byte audit-token compare + `HasBooleanEntitlement` → "Rejecting … from unentitled caller pid %d" | local sandboxed app | **CWE-862 privesc-adjacent** |
| 12 | **CoreText** `TRunGlue::DoGlyphInsertion` (u16→u64 count param) + `AppendLine`/`SetLines` CFBitVector→vector<bool> | glyph count **mod-65536 truncation** desyncs vector shift vs stored run count → glyph-storage OOB; shared mutable CFBitVector sized-once + `SetBitAtIndex` beyond count = bit-OOB/stale alias | 64-bit count throughout; ownership moved into value types | crafted web font = 1-click | heap OOB |
| 13 | **libFontParser** `TSFNTFont::GetCIDsForGlyphs` | cmap lookup index **unchecked vs map capacity** → `strh` **controlled-offset heap u16 write** | `cmp w0, w25, uxth; b.hs bail` | remote font parse (@font-face, attachments) | heap write |
| 14 | **WebCore 624.5.1 branch** | **JSTreeWalker/JSTrustedTypePolicy missing GC root-visitors** (added in 624.5.1) = moving-collector UAF class; `registerInternalFileBlobURL` replacementPath permissive sandbox-access check → categorical deny; HTMLDocumentParser resume re-entrancy helper; HTTPS-upgrade redirect-loop | GC hooks + policy deny + guarded helper | 1-click Safari | UAF / sandbox-file-read |
| 15 | **XNU exclaves** | new `exclaves_xnuproxy.c` asserts: L4 IPC buffer must not straddle pages + SK-shared RO/RW page provenance; `output_length <= EXCLAVES_STACKSHOT_BUFFER_SIZE`; new vm object_readonly fault path (`object_readonly_fault/copy_overwrite/iopl_request`, "VM object is read-only (decmpfs?)") | +3 fns = the new predicates | exclave/SK boundary | hardening |

**Negative findings (rebuild-only, byte-diff proven):** AppleSEPKeyStore, AKSAnalytics (text byte-identical), **apfs** (71,972 differing words = pure relink re-encodings, 4-insn peephole), **launchd/dyld/xpcproxy** (version stamps), **libsandbox.1.dylib** (profile-cache UUID only), **AMFI, AppleAVE2, H264H9, H9, AVD.videodecoder, AGXG18P, wifid, CoreTelephony, libMatch** (UUID/metadata), captiveagent (peephole), hfs removexattr (no-op reorder), E1000/IXGBE dexts (object-path stamps).

**Campaign tie-ins:** (1) the ImageIO TIFF `_TAG::writeToBuffer`/`copyDateTime` OOB reads are the 64740 TIFF-IFD class — write-path variant; (2) AppleAVD `resetFrameQslot` is the in-flight-slot race row K probed from the client side — now guarded kernel-side; (3) AVE2/H264H9 untouched = all v142 AVE findings still hold on 26.6.1; (4) the campaign's IOSA/M2Scaler surfaces untouched. **Follow-on candidate: row I class client probe for the OLD ImageIO scaler overflow (device is on 23G71) via CGImageSourceCreateThumbnailAtIndex with dstWidth*bpp ≥ 2³² — in-app, no daemon.**
